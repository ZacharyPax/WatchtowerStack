# Prometheus Query Language Query
# Used for alerting/dashboard purposes in Grafana or in the Prometheus Database directly on Vashti
# This query calculates the free RAM on the system and returns a number that represents the percentage of available RAM.
# Zachary Pax 2024 for Riverland Energy Cooperative

(windows_os_physical_memory_free_bytes / windows_cs_physical_memory_bytes)*100